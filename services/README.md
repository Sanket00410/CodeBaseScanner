# Services

This folder contains enterprise microservice stubs:
- `scan-orchestrator`
- `finding-service`
- `report-service`
- `identity-service`
- `integration-service`
- `audit-service`

Each service includes `app.py`, `requirements.txt`, and `Dockerfile`.
